[project]
name = "kop"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "kubernetes>=33.1.0",
    "textual>=7.4.0",
    "textual[syntax]",
    "pyte",
    "rich-pixels>=3.0.1",
    "pytest>=9.0.2",
]
